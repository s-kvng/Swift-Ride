import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Chat = () => {
  return (
    <SafeAreaView>
      <Text className="text-red-600 mt-10 font-bold text-2xl">Chat</Text>
    </SafeAreaView>
  );
};

export default Chat;
