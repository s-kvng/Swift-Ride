import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Profile = () => {
  return (
    <SafeAreaView>
      <Text className="text-red-600 mt-10 font-bold text-2xl">Profile</Text>
    </SafeAreaView>
  );
};

export default Profile;
